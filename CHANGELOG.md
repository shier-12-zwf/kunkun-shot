# 更新日志

本项目遵循语义化版本号。早期预览阶段的功能和兼容性仍可能调整。

## v0.3.0 — 2026-09-03

### 用户反馈修复

- 将“翻译”固定为截图完成后的一级首项，“OCR”紧随其后；常用宽度下无需进入“更多”即可直接翻译。
- 修复贴图偶发透明或空白：静态输出改为等待当前代背景图完成解码，图片加载失败时保留上一张有效图和明确重试入口；拖动、缩放、恢复选区及关闭面板不会绕过门禁。
- 长截图从实验性入口补齐为可恢复工作流：支持纵向/横向采集、暂停/继续、删除首/中/尾任意帧、事务式重拼、撤销/重做、删段后继续，以及固定顶部/底部区域建议与手工修正。坏帧、反向滚动或宽度/DPR 变化不会覆盖上一份正确结果。

### 截图、识别与贴图

- 精确选区支持源像素宽高、常用尺寸预设、自由/1:1/4:3/16:9 比例，以及非整数、X/Y 非等比的屏幕到源图映射。
- 新增真实高斯模糊、椭圆聚光灯、文字水印和会进入最终导出图的放大镜；预览与导出共用坐标模型，交互手柄不会写入成片。
- 条码识别增加原始全分辨率二次扫描和手动重试；离线 OCR 扩展为简中、繁中、英语、日语、韩语、法语、德语、西班牙语、葡萄牙语及相应英语组合，缺少语言包时明确失败且不访问 CDN。
- AI 表格结果可直接编辑，并可复制为 CSV、TSV 或 Markdown；CSV/TSV 输出增加公式注入防护和行列、字符上限。
- 图片贴图新增裁剪、顺/逆时针 90° 旋转、水平/垂直翻转；当前全部贴图可分组联动移动、折叠/展开、移出和自动解散。
- 新增完全离线的 KaTeX 公式窗口，可调整字号、文字色与透明/白色背景并生成普通 PNG 贴图；公式脚本、字体和许可证均随应用打包。

### 录屏、导出与可靠性

- 录屏新增点击提示、隐私收敛后的快捷键提示、摄像头画中画和写入成片的画笔；事件与裁区按真实媒体流像素分别映射 X/Y，兼容 Retina、负坐标显示器和非等比缩放。
- 截图与录屏支持安全文件名模板及同名 `-2` 起避让；图片导出覆盖 PNG、JPEG、WebP、BMP、AVIF 和单页 PDF，历史页可按选择顺序合成最多 100 页的多页 PDF。
- 在恢复任何贴图窗口前安装统一导航、新窗口和媒体权限防护，避免启动恢复窗口漏过安全策略；录屏权限只放行应用主动请求且实际需要的媒体类型。
- 使用 Node.js 22 全新安装锁定依赖，完整与生产依赖审计均为 0 漏洞；源码回归 **461/461**、正常/故障注入 Electron 冒烟、933×701 非标准视口工具栏交互和打包态冒烟全部通过。
- 固定本机证书签名的 0.3.0 APP、DMG、ZIP 已通过签名与完整性检查，并已在本机从 0.2.2 更新、启动和再次烟测；这不等同 Developer ID 签名、Apple 公证或全部真实权限/硬件场景验收。

### 分发说明

GitHub Release 仍只发布源码。当前本地 DMG/ZIP 不作为公开正式安装包上传：项目尚未完成 Developer ID、公证、异机 Gatekeeper 验证和 FFmpeg 二进制分发义务闭环。

## v0.2.2 — 2026-09-03

### 入口与操作层级

- 主窗口“快捷截图”和菜单栏图标左键快捷面板都新增可见的“长截图”入口；全局快捷键与菜单栏右键菜单入口继续保留。
- 截图编辑器的一级动作改为以“翻译”“OCR”开头，贴图与复制保持直接可用；“问 AI”、表格、公式、润色、保存等低频动作收进“更多”，翻译目标语言仍在“更多”中设置。

### 修复与自动化

- 修复静态截图背景尚未解码时立即点击“贴图”可能生成透明或空白贴图的问题：底图准备完成前不开放静态输出动作；拖动、调整选区、关闭 AI 面板或恢复最近选区后也不会绕过门禁；解码失败时会持续提示取消后重试。
- 修复成功状态码 `0` 未触发最终退出而可能让 Electron 冒烟测试超时的问题；成功和注入失败路径现在都会在正常退出清理完成后按预期状态结束。
- 新增主窗口/菜单栏长截图入口、工具栏一级动作顺序、截图背景就绪门禁及确定性冒烟退出的契约回归；运行时工具栏检查同时验证“翻译 → OCR”的可见顺序与宽度边界。
- 更新开发依赖锁定中的 `fast-uri` 与 `@xmldom/xmldom` 安全修复版本；完整及生产依赖审计均为 0 漏洞。
- 本轮源码已通过 `npm test` **347/347**、截图工具栏运行时检查，以及源码态 Electron 正常/故障注入冒烟；固定本机签名的 `.app`、DMG、ZIP 通过签名与完整性校验，打包态冒烟通过，并已在本机安装、重启为 `v0.2.2`。这仍不等同 Developer ID 签名、公证或全部真实权限场景验收。

## v0.2.1 — 2026-09-01

### 自 v0.2.0 标签后的功能补齐

- 截图导出扩展为 PNG、JPEG、WebP、BMP、AVIF 和单页 PDF；快速保存、保存对话框与历史批量导出共用格式和质量配置，并以原子写入避免残留半成品。
- 图片贴图新增画笔、直线、箭头、矩形、椭圆、文字和橡皮标注；复制、保存、OCR、AI 与拖出文件使用实际合成结果，正常退出后可恢复贴图工作区。
- AI 工作台新增表格（Markdown/CSV）与公式（LaTeX）识别，明确视觉图片与本地 OCR 文本的服务路由；本地 OCR 语言资源缺失时失败关闭，不再回退网络下载。
- 区域录屏新增系统声音与麦克风独立开关、双音源混音、暂停/继续、剪辑、超限保护与保存重试；请求的音源不可用时明确失败。
- 新增 `--capture` / `--delay` 启动参数，首次与第二次启动共用受校验的截图流程；完善窗口截图来源匹配、屏幕权限提示、多显示器坐标和延时任务取消。
- 新增稳定本机证书签名、临时 ad-hoc 测试及失败关闭的 Developer ID / 公证流水线；仓库当前仍无 Apple 分发凭据，不构成正式签名或公证声明。

### 修复与加固

- 修复 Electron 冒烟测试即使发现错误仍以成功状态退出、并遗留隔离用户目录的问题；现在同时验证成功和注入失败两条路径。
- 所有渲染窗口打开外部链接前统一执行 HTTP(S)、长度和凭据校验，拒绝 `file:` 等非网络协议。
- 修复全新 CI 安装中 Electron / Chromium 许可证文件可能尚未准备好而导致的打包失败；运行时只损坏许可证文件时也会从校验后的同版归档恢复。
- 新增 Swift 原生 helper 的安全复用路径：只接受文件名与当前源码哈希一致、包含目标 CPU 架构且可执行的 Mach-O 文件；任一条件不符即停止打包。
- 修复智能 UI 选区在探测延迟、快速开关或权限失败时可能保留旧高亮并误选的问题；无效元素尺寸会失败关闭，原生 helper 使用系统 JSON 序列化兼容引号、反斜杠和换行等标题内容。
- CI 将与提交源码哈希绑定的 macOS 原生 helper 短期保留 7 天，供本机 Swift/SDK 不匹配时完成同源验证和稳定签名构建；下载物仍须通过文件名源码哈希、CPU 架构和非符号链接检查。
- 设置页表单标签、说明与控件建立可访问关联；贴图右键菜单支持完整键盘导航和焦点恢复。
- 所有渲染窗口支持 `prefers-reduced-motion`，并收敛 AI 错误提示与加载动效。

### 自动化与发布链路

- 新增真实 Electron 隔离冒烟测试和截图工具栏交互检查，统一入口为 `npm run test:all`。
- GitHub Actions 在单元/契约测试后执行源码态 Electron 检查、依赖审计和无分发凭据的 macOS 应用打包，然后再直接启动已打包的 `.app` 完成第二轮冒烟。
- 新增 CI 工作流、外链安全、许可证恢复、Swift helper 回退和渲染层无障碍回归测试。
- 删除会读取真实用户配置且无法可靠报告失败的旧截图 E2E 脚本。

### 文档

- 新增 [完整使用指南](docs/使用指南.md)。
- 新增 [PixPin 竞品对比与改进报告](docs/PixPin-竞品对比与改进报告.md)。

### 分发说明

GitHub Release 继续仅发布源码。项目仍没有可公开分发的 Developer ID 签名、公证和 FFmpeg 二进制合规闭环，因此不会把本机自签名 DMG/ZIP 上传为正式安装包。

## v0.2.0 — 2026-08-24

- 首个公开源码预览版。
- 提供区域、窗口、全屏与延时截图、基础标注与贴图、本地 OCR、二维码识别、实验性长截图、区域录屏、历史库和可选 AI 问答/翻译/润色。
- 加固 API Key 存储、IPC 权限与输入校验、原子写入、临时文件和剪贴板恢复流程。
